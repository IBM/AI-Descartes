// © Copyright IBM Corporation 2022. All Rights Reserved.
// LICENSE: MIT License https://opensource.org/licenses/MIT
// SPDX-License-Identifier: MIT

package utils;

//import System.out;

public class Utils {
	// public static UList<Integer> rangeClosed(int start, int last) {
	// return IntUList.rangeClosed(start, last).boxed();
	// }

	//
	// public static <T> UList<T> filterActive(UList<T> xs, Predicate<T> filter,
	// String nm) {
	// final AtomicInteger nskipped = new AtomicInteger(0);
	// UList<T> ys = xs.filter(x -> {
	// if (filter.test(x))
	// return true;
	// else {
	// nskipped.incrementAndGet();
	// return false;
	// }
	// });
	// if (nskipped.get() > 0)
	// printf("SKIPPED %s non-Active %s ENTRIES%n", nskipped, nm);
	// return ys;
	// }
	//
	// public static Predicate<GameAllocation> testActive = x -> {
	// if (x.Active.trim().equals("1"))
	// return true;
	// if (!x.Active.trim().equals("0"))
	// printf("SAW UNUSUAL VALUE OF 'Active': %s%n", x.Active);
	// return false;
	// };

}
